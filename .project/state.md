# Current state

- Lifecycle: M0 through M5 and D1 through D5 are complete. D5 acceptance was recorded on 2026-08-04 under the approved free-first deployment profile. R1 dashboard remediation is complete and production-verified.
- Data: Sparkov simulated credit-card transactions v1 acquired locally and checksum-pinned. The source has 1,296,675 training rows (7,506 fraud labels) and 555,719 test rows (2,145 fraud labels); the chronological split is strictly ordered.
- Implementation: local PostgreSQL pipeline verified. Versioned migrations enforce constraints and RLS; ingestion emits only approved fields with deterministic idempotency keys and aggregate-only public views.
- Deployment: the public GitHub repository is live at https://github.com/vaibhavkhuranaaa/payments-fraud-risk-data-platform. Supabase Free project `elhhydpltongfdziroio` is provisioned in `us-east-2`; it contains exactly two source-level aggregate monitoring rows and no event-level or raw source data. On 2026-08-04, Render redeployed revision `d1f8db2` after its server-only API key was rotated; `GET /health` returned 200 with the non-sensitive `{"status":"ok"}` response. Vercel Production now holds only `API_BASE_URL` and a masked `RISK_API_KEY` as runtime configuration and deployment `BdM84XXscBy894334KVNYshPkGWK` is ready at https://payments-fraud-risk-dashboard.vercel.app.
- Publication: the protected runtime contract is verified. The public dashboard successfully renders exactly two aggregate source partitions and precomputed evaluation evidence through the server-side key; it exposes no browser-visible secret, raw source, event-level record, or scoring workflow. D5 is complete.
- Governance: raw source files are local-only under `data/raw/` and excluded by `.gitignore`. `contracts/sparkov-source.yml` restricts analytical use to timestamp, merchant, category, amount, and label; all other source fields are prohibited.
- Validation: 1,852,394 PostgreSQL events, no duplicate event IDs, no prohibited source fields in the table, and an aggregate-only public monitoring view. Integration tests verify the schema and idempotent load.
- Target deployment: Supabase PostgreSQL, Render FastAPI, and Vercel Next.js as documented in `docs/deployment-plan.md`, with a $0 monthly cap. Public API data endpoints remain server-to-server-key protected.
- Evaluation: chronological baseline/challenger evaluation is reproducible in `scripts/evaluate_models.py`; calibration bins and monthly alert volumes are captured in `data/validated/evaluation.json`. The baseline outperforms the class-weighted challenger on PR-AUC and recall at a 1% review rate.
- API and dashboard: FastAPI exposes only health, aggregate monitoring, and precomputed evaluation evidence. The Next.js dashboard consumes those contracts with loading and service-unavailable states; no event-level record or scoring endpoint is present.
- Security: `risk_api` has SELECT only on the aggregate monitoring view; it cannot read `risk.events`. The local Docker superuser is explicitly development-only.
- Release validation: four local PostgreSQL/API tests, project-record validation, Docker Compose configuration, dashboard lint, dashboard production build, and a localhost dashboard render passed on 2026-08-01. The `uv` dependency contract is verified by `uv.lock` and an isolated `.venv`; the test-only `httpx` dependency is explicitly locked. The locally built `payments-risk-api:local` image verified `/health`, `/v1/monitoring`, and `/v1/evaluation` against local PostgreSQL. Its temporary verification container was removed; unused Docker build cache was pruned.
- D5 approval: the free-first profile is approved: public GitHub under `vaibhavkhuranaaa`, Supabase Free in Ohio with aggregate-only hosted data, Render Free API, and Vercel Hobby dashboard. The GitHub repository, Supabase aggregate publication, Render API, and Vercel dashboard are provisioned. Supabase security and performance advisors returned no findings after the reader policy was applied. The API-key rotation, Render redeploy, public health check, and Vercel Production runtime contract are verified without recording or exposing credentials. No paid resources or payment method were added.
- R1 remediation: dashboard lint/build and repository tests are verified. The page adds a deterministic browser-generated synthetic stream plus aggregate evidence visuals for lineage, calibration, monthly alert volume, and model comparison. On 2026-08-04, Vercel deployment `dpl_2q8s16KyeB8DjqJC6opq6YEisTqk` deployed revision `d33f46170f8e7cb37581f4526844a7723c3cdf36` to the existing production URL. It reached READY, returned HTTP 200, and Vercel's initial runtime-error scan was clear; no provider, hosted-data, API-contract, public-exposure, or cost change occurred.
