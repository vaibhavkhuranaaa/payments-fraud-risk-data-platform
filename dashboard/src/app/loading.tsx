export default function Loading() {
  return <main style={{ minHeight: "100vh", padding: "10vh 8vw", background: "#edf1ed", color: "#152126", fontFamily: "Arial, sans-serif" }}><p style={{ color: "#005e5d", fontSize: 12, fontWeight: 800, letterSpacing: ".08em", textTransform: "uppercase" }}>Loading</p><h1>Loading aggregate evidence…</h1><p>The dashboard is waiting only for the protected aggregate service; it never substitutes raw records.</p></main>;
}
